[Definition]
# Nginx error.log에서 limit_req에 의해 차단된 로그 패턴을 감지하는 정규식
failregex = ^(?:\d{4}/\d{2}/\d{2} \d{2}:\d{2}:\d{2} )?\[error\] \d+#\d+: \*\d+ limiting requests, excess:.*, client: <HOST>,
ignoreregex =
